
const ProcessHero = () => {
  return(
    <section className="hero" id="process-hero">
      <h2>
      We understand every customer has their own use for our Sheds. Each Shed is tailored specifically to our customers’ unique needs. 
      </h2>
    </section>
  );
}

export default ProcessHero;